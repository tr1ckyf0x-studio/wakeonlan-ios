//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

final class {{ module_info.name }}ViewController: UIViewController {

    var presenter: {{ module_info.name }}ViewOutputProtocol?

    private lazy var moduleView: {{ module_info.name }}View = {
        let view = {{ module_info.name }}View()
        return view
    }()

    // MARK: - Life cycle
    override func loadView() {
        view = moduleView
    }

    override func viewDidLoad() {
        super.viewDidLoad()
        {{ module_info.name }}Configurator().configure(viewController: self)
        presenter?.viewDidLoad()
    }
}

// MARK: - {{ module_info.name }}ViewInputProtocol
extension {{ module_info.name }}ViewController: {{ module_info.name }}ViewInputProtocol {
    func setupView() {
    }
}
